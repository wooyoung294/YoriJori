/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: [
            "loremflickr.com",
            "picsum.photos"
            // ...
        //
        ]
    }
};

export default nextConfig;
